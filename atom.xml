<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://Gentleelephant.github.io</id>
    <title>Aboy</title>
    <updated>2020-03-29T09:24:53.867Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://Gentleelephant.github.io"/>
    <link rel="self" href="https://Gentleelephant.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://Gentleelephant.github.io/images/avatar.png</logo>
    <icon>https://Gentleelephant.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Aboy</rights>
    <entry>
        <title type="html"><![CDATA[项目第三步：前端请求方法的封装]]></title>
        <id>https://Gentleelephant.github.io/post/xiang-mu-di-san-bu-qian-duan-qing-qiu-fang-fa-de-feng-zhuang/</id>
        <link href="https://Gentleelephant.github.io/post/xiang-mu-di-san-bu-qian-duan-qing-qiu-fang-fa-de-feng-zhuang/">
        </link>
        <updated>2020-03-29T09:22:38.000Z</updated>
        <content type="html"><![CDATA[<p>如果我们不封装方法，那么我们每次都要import，所以我们可以把方法制作成Vue插件</p>
<p>首先在api.js中编写出剩下的方法：</p>
<pre><code class="language-javascript">export const postRequest=(url,params)=&gt;{
    return axios({
        method: 'post',
        url: `${url}`,
        data: params
    })
}

export const putRequest=(url,params)=&gt;{
    return axios({
        method: 'put',
        url: `${url}`,
        data: params
    })
}

export const getRequest=(url,params)=&gt;{
    return axios({
        method: 'get',
        url: `${url}`,
        data: params
    })
}

export const deleteRequest=(url,params)=&gt;{
    return axios({
        method: 'delete',
        url: `${url}`,
        data: params
    })
}
</code></pre>
<p>然后再main.js中导入这些方法并且制作插件：</p>
<pre><code class="language-javascript">// 导入
import {postKeyValueRequest} from &quot;../utils/api&quot;;
import {postRequest} from &quot;../utils/api&quot;;
import {putRequest} from &quot;../utils/api&quot;;
import {getRequest} from &quot;../utils/api&quot;;
import {deleteRequest} from &quot;../utils/api&quot;;

// 制作插件
Vue.prototype.postRequest = postRequest;
Vue.prototype.putRequest = putRequest;
Vue.prototype.getRequest = getRequest;
Vue.prototype.deleteRequest = deleteRequest;
Vue.prototype.postKeyValueRequest = postKeyValueRequest;
</code></pre>
<p>这样在我们想用这些方法的时候是需要用 this. 的形式调用了</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ 项目第二步：前后端接口对接]]></title>
        <id>https://Gentleelephant.github.io/post/xiang-mu-qian-hou-duan-jie-kou-dui-jie/</id>
        <link href="https://Gentleelephant.github.io/post/xiang-mu-qian-hou-duan-jie-kou-dui-jie/">
        </link>
        <updated>2020-03-29T09:07:50.000Z</updated>
        <content type="html"><![CDATA[<p>网络请求使用axios，首先安装axios，在控制台下输入<code>npm install axios</code>如果在package.js中看到:</p>
<pre><code class="language-json"> &quot;dependencies&quot;: {
    &quot;axios&quot;: &quot;^0.19.2&quot;, // axios已经安装成功
    &quot;core-js&quot;: &quot;^3.6.4&quot;,
    &quot;element-ui&quot;: &quot;^2.13.0&quot;,
    &quot;vue&quot;: &quot;^2.6.11&quot;,
    &quot;vue-router&quot;: &quot;^3.1.5&quot;
  },
</code></pre>
<p>编写网络请求方法：</p>
<p>api.js</p>
<pre><code class="language-javascript">import axios from 'axios'
import {Message} from 'element-ui';

axios.interceptors.response.use(success =&gt; {
    if (success.status &amp;&amp; success.status == 200 &amp;&amp; success.data.status == 500) {
        Message.error({message: success.data.msg})
        return;
    }
    return success.data;
}, error =&gt; {
    if (error.response.status == 504 || error.response.status == 404) {
        alert(error.response.data)
        Message.error(&quot;服务器被吃了&quot;);
    } else if (error.response.status == 403) {
        Message.error(&quot;权限不足&quot;)
    } else if (error.response.status == 401) {
        Message.error(&quot;没有登陆&quot;)
    } else {

        if (error.response.data.msg) {
            Message.error({message: error.response.data.msg});
        } else {
            Message.error(&quot;未知错误&quot;);
        }
    }

    return;
})


let base = '';

export const postKeyValueRequest = (url, params) =&gt; {
    return axios({
        method: 'post',
        url: url,
        data: params,
        transformRequest: [function (data) {
            let ret = '';
            for (let i in data) {
                ret += encodeURIComponent(i) + '=' + encodeURIComponent(data[i]) + '&amp;'
                // console.log(i + &quot;:&quot; + data[i])
            }
            ret = ret.substring(0, ret.length - 1)
            console.log(ret);
            return ret;
        }],
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded'
        }
    })
}

</code></pre>
<p>这里编写完成之后还不能使用因为存在跨域问题，因此我们还要编写配置</p>
<p>vue.config.js(项目目录下)</p>
<pre><code class="language-javascript">let proxyObj = {};
proxyObj['/'] = {
    ws: false,
    target: 'http://localhost:8081',
    changeOrigin: true,
    pathRewrite: {
        '^/': ''
    }
}

module.exports = {
    devServer: {
        host: 'localhost',
        port: 8080,	// 本项目的端口
        proxy: proxyObj	// 代理配置
    }
}
</code></pre>
<p>实现submitLogin提交按钮的方法：</p>
<pre><code class="language-javascript">methods: {
            submitLogin() {
                this.$refs.login.validate((valid) =&gt; {
                    if (valid) {
                        postKeyValueRequest('/doLogin',this.loginForm).then(resp=&gt;{
                            if (resp) { // 服务端返回的数据
                                window.sessionStorage.setItem(&quot;user&quot;,JSON.stringify(resp.obj));
                                // 页面跳转
                                this.$router.replace('/home')
                                // alert(JSON.stringify(resp));
                            }
                        })
                    } else {
                        this.$message.error(&quot;请输入所有参数&quot;);
                        return false;
                    }
                });
            }
        }
</code></pre>
<p>测试访问：</p>
<p>登录页：</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200329170543.png" alt="" loading="lazy"></figure>
<p>登陆成功，页面跳转：</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200329170649.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[项目第一步：数据库及Security配置]]></title>
        <id>https://Gentleelephant.github.io/post/Project-001/</id>
        <link href="https://Gentleelephant.github.io/post/Project-001/">
        </link>
        <updated>2020-03-23T12:12:51.000Z</updated>
        <content type="html"><![CDATA[<h4 id="一-数据库表">一、数据库表</h4>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/%E7%AC%AC15%E9%A1%B5.jpg" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/%E7%AC%AC16%E9%A1%B5.jpg" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/%E7%AC%AC17%E9%A1%B5.jpg" alt="" loading="lazy"></figure>
<h4 id="二-springsecurity配置">二、SpringSecurity配置</h4>
<p>pom依赖</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;
    &lt;artifactId&gt;druid-spring-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;1.1.10&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.mybatis.spring.boot&lt;/groupId&gt;
    &lt;artifactId&gt;mybatis-spring-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;2.1.1&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;mysql&lt;/groupId&gt;
    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;
    &lt;scope&gt;test&lt;/scope&gt;
    &lt;exclusions&gt;
        &lt;exclusion&gt;
            &lt;groupId&gt;org.junit.vintage&lt;/groupId&gt;
            &lt;artifactId&gt;junit-vintage-engine&lt;/artifactId&gt;
        &lt;/exclusion&gt;
    &lt;/exclusions&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-logging&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.security&lt;/groupId&gt;
    &lt;artifactId&gt;spring-security-test&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p>application.yml配置文件中的配置</p>
<pre><code class="language-yaml">server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vhr?serverTimezone=GMT&amp;useUnicode=true&amp;characterEncoding=UTF-8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
</code></pre>
<p>实体类:</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200323200722.png" alt="" loading="lazy"></figure>
<p>Mapper接口和Mapper.xml:</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200323200932.png" alt="" loading="lazy"></figure>
<p>HrService类：</p>
<pre><code class="language-java">@Service
public class HrService implements UserDetailsService {

    @Autowired
    HrMapper hrMapper;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
        Hr hr = hrMapper.loadUserByUsername(username);
        if (hr == null){
            System.out.println(&quot;没有查询到用户&quot;);
            throw new UsernameNotFoundException(&quot;用户不存在!&quot;);
        }
        return hr;
    }
}
</code></pre>
<p>SecurityConfig配置类：</p>
<pre><code class="language-java">@Configuration
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Autowired
    HrService hrService;

    @Bean
    PasswordEncoder passwordEncoder(){
        return new BCryptPasswordEncoder();
    }


    @Override
    protected void configure(AuthenticationManagerBuilder auth) throws Exception {
        auth.userDetailsService(hrService);
    }

    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http.authorizeRequests()
                .anyRequest().authenticated()
                .and()
                .formLogin()
                .usernameParameter(&quot;username&quot;)
                .passwordParameter(&quot;password&quot;)
                .loginProcessingUrl(&quot;/doLogin&quot;)     // 表单登录提交请求
                .loginPage(&quot;/login&quot;)                // 登陆页面
                .successHandler(new AuthenticationSuccessHandler() {    // 登陆成功的方法
                    @Override
                    public void onAuthenticationSuccess(HttpServletRequest request, HttpServletResponse response, Authentication authentication) throws IOException, ServletException {
                        response.setContentType(&quot;application/json;charset=utf-8&quot;);
                        PrintWriter out = response.getWriter();
                        Hr principal = (Hr) authentication.getPrincipal();
                        RespBean ok = RespBean.ok(&quot;登陆成功&quot;,principal);
                        String string = new ObjectMapper().writeValueAsString(ok);
                        out.write(string);
                        out.flush();
                        out.close();
                    }
                })
                .failureHandler(new AuthenticationFailureHandler() {
                    @Override
                    public void onAuthenticationFailure(HttpServletRequest request, HttpServletResponse response, AuthenticationException e) throws IOException, ServletException {
                        response.setContentType(&quot;application/json;charset=utf-8&quot;);
                        PrintWriter out = response.getWriter();
                        RespBean respBean = RespBean.error(&quot;登陆失败&quot;);
                        if (e instanceof LockedException){
                            respBean.setMsg(&quot;账户被锁定,请联系管理员&quot;);
                        }else if (e instanceof CredentialsExpiredException){
                            respBean.setMsg(&quot;密码过期,请联系管理员&quot;);
                        }else if (e instanceof AccountExpiredException){
                            respBean.setMsg(&quot;账户过期,请联系管理员&quot;);
                        }else if (e instanceof DisabledException){
                            respBean.setMsg(&quot;账户被禁用,请联系管理员&quot;);
                        }else if (e instanceof BadCredentialsException){
                            respBean.setMsg(&quot;用户名或者密码输入错误&quot;);
                        }
                        String string = new ObjectMapper().writeValueAsString(respBean);
                        out.write(string);
                        out.flush();
                        out.close();
                    }
                })
                .permitAll()
                .and()
                .logout()
                .logoutSuccessHandler(new LogoutSuccessHandler() {
                    @Override
                    public void onLogoutSuccess(HttpServletRequest request, HttpServletResponse response, Authentication authentication) throws IOException, ServletException {

                    }
                })
                .permitAll()
                .and()
                .csrf().disable();
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[基于数据库的认证]]></title>
        <id>https://Gentleelephant.github.io/post/SpringSecurity-db/</id>
        <link href="https://Gentleelephant.github.io/post/SpringSecurity-db/">
        </link>
        <updated>2020-03-21T14:11:28.000Z</updated>
        <content type="html"><![CDATA[<h4 id="一-创建数据库以及表">一、创建数据库以及表</h4>
<p>数据库中有三张表，role(角色)，user(用户)，user_role（关联）</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321213935.png" alt="" loading="lazy"></figure>
<p>role表：</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321214028.png" alt="" loading="lazy"></figure>
<p>其中id为主键且自增，name是角色的name，namezh是中文名。</p>
<p>user表：</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321214143.png" alt="" loading="lazy"></figure>
<p>user是用户名，password是在SpringSecurity中用BCryptPasswordEncoder生成的密码，enable表示账户是否可用，locked表示账户是否被锁定。</p>
<p>user_role表：</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321214523.png" alt="" loading="lazy"></figure>
<p>uid是user的id，rid是role的id，一个用户可能有多个角色。</p>
<h4 id="二-创建项目">二、创建项目</h4>
<ul>
<li>导入依赖</li>
</ul>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;org.mybatis.spring.boot&lt;/groupId&gt;
    &lt;artifactId&gt;mybatis-spring-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;2.1.1&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;
    &lt;artifactId&gt;druid-spring-boot-starter&lt;/artifactId&gt;
    &lt;version&gt;1.1.10&lt;/version&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
    &lt;groupId&gt;mysql&lt;/groupId&gt;
    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p>编写配置：</p>
<pre><code class="language-yaml">spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codezp?serverTimezone=GMT&amp;useUnicode=true&amp;characterEncoding=UTF-8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
</code></pre>
<ul>
<li>
<p>创建User和Role实体类</p>
<p>User类：</p>
<pre><code class="language-java">public class User implements UserDetails {

    private Integer id;

    private String username;

    private String password;

    private Boolean enable;

    private Boolean locked;

    private List&lt;Role&gt; roles;

    public List&lt;Role&gt; getRoles() {
        return roles;
    }

    public void setRoles(List&lt;Role&gt; roles) {
        this.roles = roles;
    }

    public Integer getId() {
        return id;
    }

    public void setId(Integer id) {
        this.id = id;
    }

    @Override
    public String getUsername() {
        return username;
    }


    // 账户是否未过期
    @Override
    public boolean isAccountNonExpired() {
        return true;
    }

    // 账户是否未锁定
    @Override
    public boolean isAccountNonLocked() {
        return !locked;
    }

    // 凭证是否未过期(密码)
    @Override
    public boolean isCredentialsNonExpired() {
        return true;
    }

    @Override
    public boolean isEnabled() {
        return enable;
    }

    public void setUsername(String username) {
        this.username = username;
    }

    // 返回用户的所有角色
    @Override
    public Collection&lt;? extends GrantedAuthority&gt; getAuthorities() {
        List&lt;SimpleGrantedAuthority&gt; authorities = new ArrayList&lt;&gt;();
        for (Role role : roles) {
            // role必须以&quot;ROLE_&quot;开始
            authorities.add(new SimpleGrantedAuthority(&quot;ROLE_&quot; + role.getName()));

        }
        return authorities;
    }

    @Override
    public String getPassword() {
        return password;
    }

    public void setPassword(String password) {
        this.password = password;
    }

    public void setEnable(Boolean enable) {
        this.enable = enable;
    }

    public void setLocked(Boolean locked) {
        this.locked = locked;
    }
}
</code></pre>
<p>我们的类要想到时候能被SpringSecurity使用，必须实现UserDetails。</p>
<p>Role类：</p>
<pre><code class="language-java">public class Role {

    private Integer id;

    private String name;

    private String namezh;

    public Integer getId() {
        return id;
    }

    public void setId(Integer id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getNamezh() {
        return namezh;
    }

    public void setNamezh(String namezh) {
        this.namezh = namezh;
    }
}

</code></pre>
</li>
</ul>
<h4 id="第三步-编写mybatis的mapper接口和实现">第三步、编写Mybatis的Mapper接口和实现</h4>
<p>UserMapper:</p>
<pre><code class="language-java">@Mapper
public interface UserMapper {
	// 根据用户名获得用户对象
    User loadUserByUsername(String username);
	// 根据用户id获得用户的Role
    List&lt;Role&gt; getUserRolesById(Integer id);

}

</code></pre>
<p>UserMapper.xml</p>
<pre><code class="language-xml">&lt;!DOCTYPE mapper
        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;
        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;

&lt;mapper namespace=&quot;com.codezp.mapper.UserMapper&quot;&gt;
    &lt;select id=&quot;loadUserByUsername&quot; resultType=&quot;com.codezp.bean.User&quot;&gt;
        select * from user where username=#{username}
    &lt;/select&gt;

    &lt;select id=&quot;getUserRolesById&quot; resultType=&quot;com.codezp.bean.Role&quot;&gt;
        select * from role where id in (select rid from user_role where uid=#{id})
    &lt;/select&gt;

&lt;/mapper&gt;
</code></pre>
<h4 id="第四步-编写userservice类以及security配置类">第四步、编写Userservice类以及Security配置类</h4>
<p>这个类到时候要交给SpringSecurity的配置类使用。</p>
<pre><code class="language-java">@Service
public class UserService implements UserDetailsService {

    @Autowired
    UserMapper userMapper;

    @Override
    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {

        User user = userMapper.loadUserByUsername(username);
        if (user == null){
            throw new UsernameNotFoundException(&quot;user not found&quot;);
        }
        user.setRoles(userMapper.getUserRolesById(user.getId()));
        return user;
    }
}
</code></pre>
<p>SpringSecurity的配置类</p>
<pre><code class="language-java">@Configuration
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Autowired
    UserService userService;

    @Bean
    PasswordEncoder passwordEncoder(){
        return new BCryptPasswordEncoder();
    }

    @Override
    protected void configure(AuthenticationManagerBuilder auth) throws Exception {
        // 刚才写的UserService类的对象
        auth.userDetailsService(userService);
    }

    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http.authorizeRequests()
                .antMatchers(&quot;/dba/**&quot;).hasRole(&quot;dba&quot;)
                .antMatchers(&quot;/admin/**&quot;).hasRole(&quot;admin&quot;)
                .antMatchers(&quot;/user/**&quot;).hasRole(&quot;user&quot;)
                .anyRequest().authenticated()
                .and()
                .formLogin()
                .permitAll()
                .and()
                .csrf().disable();
    }
}
</code></pre>
<h5 id="第五步-编写controller进行测试">第五步、编写Controller进行测试</h5>
<pre><code class="language-java">@RestController
public class HelloController {

    @GetMapping(&quot;/hello&quot;)
    public String hello(){
        return &quot;hello serurity&quot;;
    }

    @GetMapping(&quot;/dba/hello&quot;)
    public String dba(){
        return &quot;hello dba&quot;;
    }

    @GetMapping(&quot;/admin/hello&quot;)
    public String root(){
        return &quot;hello admin&quot;;
    }

    @GetMapping(&quot;/user/hello&quot;)
    public String user(){
        return &quot;hello user&quot;;
    }

}
</code></pre>
<p>root有dba和admin的权限，它应该能访问到dba和admin下的资源。</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321220908.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321221002.png" alt="" loading="lazy"></figure>
<figure data-type="image" tabindex="7"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321221037.png" alt="" loading="lazy"></figure>
<p>可以看到在使用root用户登录之后可以访问到dba和admin下的资源，但是访问不到user下的资源。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringSecurity的配置（自定义拦截规则）]]></title>
        <id>https://Gentleelephant.github.io/post/httpsecurity-custom/</id>
        <link href="https://Gentleelephant.github.io/post/httpsecurity-custom/">
        </link>
        <updated>2020-03-21T07:28:27.000Z</updated>
        <content type="html"><![CDATA[<h4 id="一-简单的配置">一、简单的配置</h4>
<p>重写一个configure方法参数是AuthenticationManagerBuilder类型的,用来配置用户名和密码，而且还要添加一个密码编码器(Spring5之后密码必须要加密)。</p>
<pre><code class="language-java">@Bean
    PasswordEncoder passwordEncoder() {
        return NoOpPasswordEncoder.getInstance();
    }


    @Override
    protected void configure(AuthenticationManagerBuilder auth) throws Exception {
        auth.inMemoryAuthentication()
                .withUser(&quot;codezp&quot;).password(&quot;123&quot;).roles(&quot;admin&quot;)
                .and()
                .withUser(&quot;haha&quot;).password(&quot;456&quot;).roles(&quot;user&quot;);
    }
</code></pre>
<p>重写另一个configure方法，添加自己的拦截规则</p>
<pre><code class="language-java">    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http.authorizeRequests()
                .antMatchers(&quot;/admin/**&quot;).hasRole(&quot;admin&quot;)
                //.antMatchers(&quot;user/**&quot;).hasAnyRole(&quot;admin&quot;,&quot;user&quot;)
                //上面注释的方式和下面的方式都可以
                .antMatchers(&quot;/user/**&quot;).access(&quot;hasAnyRole('user','admin')&quot;)
                .anyRequest().authenticated()  // 其他的请求都可以访问
                .and()
                .formLogin()
                .loginProcessingUrl(&quot;/doLogin&quot;) // 处理登陆请求的地址，(使用postman可以配一下)
                .permitAll()    // 跟登陆相关的接口直接能访问
                .and()
                .csrf().disable();  // 关闭csrf攻击，方便测试
    }
</code></pre>
<p>在controller中补充：</p>
<pre><code class="language-java">    @GetMapping(&quot;/admin/hello&quot;)
    public String admin(){
        return &quot;hello admin&quot;;
    }
    @GetMapping(&quot;/user/hello&quot;)
    public String user(){
        return &quot;hello user&quot;;
    }
</code></pre>
<p>现在再去浏览器访问：</p>
<p>我们用的是haha用户登陆上去的，它是user角色，所以可以访问user/**下的资源。</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321122541.png" alt="" loading="lazy"></figure>
<p>我们试着去访问admin下的资源，就会报错。</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321122942.png" alt="" loading="lazy"></figure>
<p>admin角色用户的测试就不进行，道理都是一样的。</p>
<h4 id="二-登陆表单详细配置">二、登陆表单详细配置</h4>
<pre><code class="language-java">@Override
    protected void configure(HttpSecurity http) throws Exception {
        http.authorizeRequests()
                .antMatchers(&quot;/admin/**&quot;).hasRole(&quot;admin&quot;)
//                .antMatchers(&quot;user/**&quot;).hasAnyRole(&quot;admin&quot;,&quot;user&quot;)
                .antMatchers(&quot;/user/**&quot;).access(&quot;hasAnyRole('user','admin')&quot;)
                .anyRequest().authenticated()  // 其他的请求都可以访问
                .and()
                .formLogin()
                .loginProcessingUrl(&quot;/doLogin&quot;) // 处理登陆请求的地址，(使用postman可以配一下)
                .loginPage(&quot;/login&quot;) //登陆页面
                .usernameParameter(&quot;uname&quot;) // username参数名字
                .passwordParameter(&quot;passwd&quot;) // password参数名字
                .successHandler(new AuthenticationSuccessHandler() {    // 登录成功的处理
                    @Override
                    public void onAuthenticationSuccess(HttpServletRequest req, HttpServletResponse resp, Authentication authentication) throws IOException, ServletException {
                        resp.setContentType(&quot;application/json;charset=utf-8&quot;);
                        PrintWriter writer = resp.getWriter();
                        Map&lt;String,Object&gt; map = new HashMap&lt;&gt;();
                        map.put(&quot;status&quot;, 200);
                        // authentication.getPrincipal()返回的是刚才登录成功的对象
                        map.put(&quot;msg&quot;, authentication.getPrincipal());
                        writer.write(new ObjectMapper().writeValueAsString(map));
                        writer.flush();
                        writer.close();
                    }
                })
                .failureHandler(new AuthenticationFailureHandler() {   // 登陆失败的处理
                    @Override
                    public void onAuthenticationFailure(HttpServletRequest req, HttpServletResponse resp, AuthenticationException e) throws IOException, ServletException {
                        resp.setContentType(&quot;application/json;charset=utf-8&quot;);
                        PrintWriter writer = resp.getWriter();
                        Map&lt;String,Object&gt; map = new HashMap&lt;&gt;();
                        map.put(&quot;status&quot;, 401);

                        if (e instanceof LockedException){  // 账户锁定异常
                            map.put(&quot;msg&quot;, &quot;账户被锁定，登陆失败&quot;);
                        } else if (e instanceof BadCredentialsException){ // 错误凭证
                            map.put(&quot;msg&quot;, &quot;用户名或密码输入错误,登陆失败&quot;);
                        } else if (e instanceof DisabledException){  // 账户被禁用
                            map.put(&quot;msg&quot;, &quot;账户被禁用,登陆失败&quot;);
                        } else if (e instanceof AccountExpiredException){  // 账户过期
                            map.put(&quot;msg&quot;, &quot;账户过期,登陆失败&quot;);
                        } else if (e instanceof CredentialsExpiredException){ // 密码过期
                            map.put(&quot;msg&quot;, &quot;密码过期,登陆失败&quot;);
                        }

                        writer.write(new ObjectMapper().writeValueAsString(map));
                        writer.flush();
                        writer.close();
                    }
                })
                .permitAll()    // 跟登陆相关的接口直接能访问
                .and()
                .csrf().disable();  // 关闭csrf攻击，方便测试
    }
</code></pre>
<p>使用postman进行测试：</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321142215.png" alt="" loading="lazy"></figure>
<p>把用户名和密码参数名跟换后</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321142356.png" alt="" loading="lazy"></figure>
<h4 id="三-注销登录配置">三、注销登录配置</h4>
<p>继续在configure类中添加配置</p>
<pre><code class="language-java">.and()
    .logout()
    .logoutUrl(&quot;/logout&quot;)
    .logoutSuccessHandler(new LogoutSuccessHandler() {
        @Override
        public void onLogoutSuccess(HttpServletRequest req, HttpServletResponse resp, Authentication authentication) throws IOException, ServletException {
            resp.setContentType(&quot;application/json;charset=utf-8&quot;);
            PrintWriter writer = resp.getWriter();
            Map&lt;String,Object&gt; map = new HashMap&lt;&gt;();
            map.put(&quot;status&quot;, 200);
            map.put(&quot;msg&quot;, &quot;注销成功&quot;);
            writer.write(new ObjectMapper().writeValueAsString(map));
            writer.flush();
            writer.close();
        }
    })
.and()
</code></pre>
<figure data-type="image" tabindex="5"><img src="C:%5CUsers%5C%E5%BC%A0%E9%B9%8F%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20200321143204960.png" alt="image-20200321143204960" loading="lazy"></figure>
<h4 id="四-多个httpsecurity">四、多个HttpSecurity</h4>
<pre><code class="language-java">@Configuration
public class MultiHttpSecurity {
    @Bean
    PasswordEncoder passwordEncoder(){
        return NoOpPasswordEncoder.getInstance();
    }

    @Autowired
    protected void configure(AuthenticationManagerBuilder auth) throws Exception {
        auth.inMemoryAuthentication()
                .withUser(&quot;codezp&quot;).password(&quot;111&quot;).roles(&quot;admin&quot;)
                .and()
                .withUser(&quot;haha&quot;).password(&quot;222&quot;).roles(&quot;user&quot;);
    }

    @Configuration
    @Order(value = 1)
    public static class AdminSecurity extends WebSecurityConfigurerAdapter{
        @Override
        protected void configure(HttpSecurity http) throws Exception {
            http.antMatcher(&quot;/admin/**&quot;).authorizeRequests().anyRequest().hasRole(&quot;admin&quot;);
        }
    }

    @Configuration
    @Order
    public static class OtherSecurity extends WebSecurityConfigurerAdapter{
        @Override
        protected void configure(HttpSecurity http) throws Exception {
            http.authorizeRequests().anyRequest().authenticated()
                    .and()
                    .formLogin()
                    .loginProcessingUrl(&quot;/doLogin&quot;)
                    .permitAll()
                    .and()
                    .formLogin()
                    .and()
                    .csrf()
                    .disable();
        }
    }
}

</code></pre>
<p>多个HttpSecurity也需要密码编码器。@Order是个HttpSecurity设置优先级，数字越大优先级越小。</p>
<p>可以看到已经登陆成功</p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321151623.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringSecurity初体验]]></title>
        <id>https://Gentleelephant.github.io/post/springsecurity-chu-ti-yan/</id>
        <link href="https://Gentleelephant.github.io/post/springsecurity-chu-ti-yan/">
        </link>
        <updated>2020-03-21T07:27:17.000Z</updated>
        <content type="html"><![CDATA[<h4 id="第一步先创建一个springboot项目添加依赖">第一步：先创建一个springboot项目添加依赖</h4>
<pre><code class="language-xml">&lt;dependency&gt;
      &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
      &lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;
&lt;/dependency&gt;
&lt;dependency&gt;
      &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
      &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<h4 id="第二步写一个controller测试">第二步：写一个controller测试</h4>
<pre><code class="language-java">@RestController
public class HelloController {

    @GetMapping(&quot;/hello&quot;)
    public String hello(){
        return &quot;hello security!&quot;;
    }
}
</code></pre>
<h4 id="第三步进行测试">第三步：进行测试</h4>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321113409.png" alt="" loading="lazy"></figure>
<p>我们虽然访问的是：http://localhost:8080/hello但是会被重定向成http://localhost:8080/login，需要先登录从才能访问。</p>
<p>默认的用户名是user,密码在项目启动的控制台。输入用户名和密码就能访问。</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200321113956.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringBoot实现session共享]]></title>
        <id>https://Gentleelephant.github.io/post/springboot-session/</id>
        <link href="https://Gentleelephant.github.io/post/springboot-session/">
        </link>
        <updated>2020-03-19T13:12:46.000Z</updated>
        <content type="html"><![CDATA[<h4 id="第一步导入依赖">第一步：导入依赖</h4>
<pre><code class="language-xml">&lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
		&lt;/dependency&gt;
		&lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;
		&lt;/dependency&gt;
		&lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
		&lt;/dependency&gt;
		&lt;dependency&gt;
			&lt;groupId&gt;org.springframework.session&lt;/groupId&gt;
			&lt;artifactId&gt;spring-session-data-redis&lt;/artifactId&gt;
		&lt;/dependency&gt;
</code></pre>
<h4 id="第二步编写配置文件">第二步：编写配置文件</h4>
<pre><code class="language-yaml">spring:
  redis:
    host: 192.168.202.132
    port: 6379
    password: root
    database: 0
server:
  # 这里指定端口是因为等一下要早controller中取到port方便测试
  port: 8085
</code></pre>
<h4 id="第三步编写controller测式">第三步：编写controller测式</h4>
<pre><code class="language-java">@RestController
public class HelloController {

    @Value(&quot;${server.port}&quot;)
    Integer port;

    @GetMapping(&quot;/set&quot;)
    public String set(HttpSession session){
        session.setAttribute(&quot;name&quot;, &quot;codezp&quot;);
        return String.valueOf(port);
    }

    @GetMapping(&quot;/get&quot;)
    public String get(HttpSession session){
        return (String) session.getAttribute(&quot;name&quot;) + port;
    }
}
</code></pre>
<h4 id="第四步启动项目开始测试">第四步：启动项目开始测试</h4>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319205646.png" alt="" loading="lazy"></figure>
<p>先把maven的test关掉，然后再点击package进行打包。</p>
<p>打包成功后：</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319205900.png" alt="" loading="lazy"></figure>
<p>我们控制台启动项目，注意一个可以不指定端口，但另一个需要指定端口(方便查看效果)</p>
<p><code>java -jar demosessionshare-0.0.1-SNAPSHOT.jar --server.port=8088</code></p>
<p><code>java -jar demosessionshare-0.0.1-SNAPSHOT.jar</code></p>
<p>项目启动后我们在浏览器进行测试：</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319210318.png" alt="" loading="lazy"></figure>
<p>需要先登录，我们根据访问的端口取控制台查看密码，username默认是user</p>
<p>输入密码之后：</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319210540.png" alt="" loading="lazy"></figure>
<p>我们再访问另一个项目的get方法：</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319210646.png" alt="" loading="lazy"></figure>
<p>这一次访问可以取到session的值，而且还不需要输入密码。再去redis上查看：已经有东西了。</p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200319210849.png" alt="" loading="lazy"></figure>
<p>总结：SpringBoot实现session其实就是做了一些自动化的配置，如果想要在SpringMVC中做也是可以实现的，配置也没有那么麻烦。在SpringBoot中实现session跟平常使用session没什么两样。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringBoot整合Redis]]></title>
        <id>https://Gentleelephant.github.io/post/springboot-zheng-he-redis/</id>
        <link href="https://Gentleelephant.github.io/post/springboot-zheng-he-redis/">
        </link>
        <updated>2020-03-17T05:43:23.000Z</updated>
        <content type="html"><![CDATA[<ol>
<li>
<p>SpringBoot整合Redis前提要先进行配置</p>
</li>
<li>
<ul>
<li>配置让所有主机都可以访问</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317131655.png" alt="配置让所有主机都可以访问" loading="lazy"></figure>
<ul>
<li>
<p>配置密码，requirepass不要变，后面的root可以换为你自己设置的密码</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317131952.png" alt="" loading="lazy"></figure>
</li>
</ul>
</li>
<li>
<p>需要导入的依赖：</p>
<pre><code class="language-xml">     &lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;
     &lt;/dependency&gt;
     &lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-security&lt;/artifactId&gt;
		&lt;/dependency&gt;
		&lt;dependency&gt;
			&lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
			&lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
		&lt;/dependency&gt;
</code></pre>
<p><em>因为springboot现在要求必须使用springboot security所以redis才要设置密码</em></p>
<ol>
<li>编写配置文件</li>
</ol>
<pre><code class="language-yaml">spring:
  redis:
    host: 192.168.202.132
    database: 0
    port: 6379
    password: root

</code></pre>
</li>
<li>
<p>查看RedisAutoConfiguration类</p>
</li>
</ol>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317132526.png" alt="" loading="lazy"></figure>
<p>这个类为我们准备了两个Template，第一个Template顾名思义，键-值都是Object类型的，而第二个的键-值都是String类型的。我们使用第二个。</p>
<ol start="6">
<li>
<p>编写一个测试的Controller</p>
<pre><code class="language-java">@RestController
public class HelloController {

    @Autowired
    StringRedisTemplate stringRedisTemplate;
    @GetMapping(&quot;/set&quot;)
    public void set(){
        // 注意对值的操作需要用opsXXX操作获取，而对键的操作就用stringRedisTemplate就可以了
        ValueOperations&lt;String, String&gt; stringStringValueOperations = stringRedisTemplate.opsForValue();
        stringStringValueOperations.set(&quot;name&quot;, &quot;codezp&quot;);

    }
    @GetMapping(&quot;/get&quot;)
    public void get(){
        ValueOperations&lt;String, String&gt; stringStringValueOperations = stringRedisTemplate.opsForValue();
        System.out.println(stringStringValueOperations.get(&quot;name&quot;));
    }

}
</code></pre>
<ol start="7">
<li>
<p>启动项目，并访问localhost:8080/set</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317133157.png" alt="" loading="lazy"></figure>
</li>
</ol>
</li>
</ol>
<p>密码在控制台：</p>
<p>​		<img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317133439.png" alt="" loading="lazy"></p>
<p>访问成功我们在redis上看看：</p>
<p>可以看到我们的数据已经存储上去了</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200317134034.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Redis安装]]></title>
        <id>https://Gentleelephant.github.io/post/redis-install/</id>
        <link href="https://Gentleelephant.github.io/post/redis-install/">
        </link>
        <updated>2020-03-15T03:23:30.000Z</updated>
        <content type="html"><![CDATA[<h3 id="一-编译安装前提要有gccg环境">一、编译安装(前提要有gcc/g++环境)</h3>
<ol>
<li>
<p>进入官网复制链接：<a href="http://download.redis.io/releases/redis-5.0.8.tar.gz">http://download.redis.io/releases/redis-5.0.8.tar.gz</a></p>
</li>
<li>
<p>在Linux下使用wget http://download.redis.io/releases/redis-5.0.8.tar.gz 命令下载Redis</p>
</li>
<li>
<p>然后使用<code>tar -zxvf redis-5.0.5.tar.gz</code>命令解压</p>
</li>
<li>
<p>执行<code>make</code></p>
</li>
<li>
<p>执行<code>meke install</code></p>
</li>
<li>
<p>安装完成后启动redis：<code>redis-server redis-conf</code></p>
</li>
</ol>
<h3 id="二-通过docker安装centos">二、通过docker安装(centos)</h3>
<p>docker安装好之后，先启动docker，然后直接运行命令：</p>
<p><code>docker run --name codezp-redis -d -p 6379:6379 redis --requirepass root</code></p>
<p>docker上的redis启动成功后，<strong>在宿主机器上有redis-cli的情况下</strong>可以直接运行命令连接：</p>
<p><code>redis-cli -a 123</code></p>
<p>如果宿主机器没有redis-cli也可以运行命令：</p>
<p><code>docker exec -it codezp-redis redis-cli -a root</code></p>
<h3 id="三-通过命令直接安装ubuntu">三、通过命令直接安装(Ubuntu)</h3>
<ol>
<li>
<p><code>apt-get update</code>(root用户，不是root用户需要先su，或者sudo)</p>
</li>
<li>
<p><code>apt-get install redis-server</code></p>
</li>
<li>
<p>启动redis<code>redis-server</code></p>
</li>
<li>
<p>客户端连接<code>redis-cli</code></p>
</li>
<li>
<p>出现：127.0.0.1:6379&gt; 就成功了</p>
</li>
<li>
<p>可以在  /etc/redis目录下修改redis.conf文件</p>
</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[SpringBoot异常处理策略]]></title>
        <id>https://Gentleelephant.github.io/post/springboot-exception/</id>
        <link href="https://Gentleelephant.github.io/post/springboot-exception/">
        </link>
        <updated>2020-03-14T13:47:10.000Z</updated>
        <content type="html"><![CDATA[<h2 id="自定义异常处理">自定义异常处理</h2>
<p>在 Spring Boot 项目中 ，异常统一处理，可以使用 Spring 中 @ControllerAdvice 来统一处理，也可以自己来定义异常处理方案。Spring Boot 中，对异常的处理有一些默认的策略，我们分别来看。默认情况下，Spring Boot 中的异常页面 是这样的：</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314210105.png" alt="" loading="lazy"></figure>
<p>之所以用户看到这个页面，是因为开发者没有明确提供一个 /error 路径，如果开发者提供了 /error 路径 ，这个页面就不会展示出来，不过在 Spring Boot 中，提供 /error 路径实际上是下下策，Spring Boot 本身在处理异常时，也是当所有条件都不满足时，才会去找 /error 路径。那么我们就先来看看，在 Spring Boot 中，如何自定义 error 页面，整体上来说，可以分为两种，一种是静态页面，另一种是动态页面。</p>
<p><font color="green" size=5>一、静态异常页面</font></p>
<p>自定义静态异常页面，又分为两种，第一种 是使用 HTTP 响应码来命名页面，例如 404.html、405.html、500.html ....，另一种就是直接定义一个 4xx.html，表示400-499 的状态都显示这个异常页面，5xx.html 表示 500-599 的状态显示这个异常页面。默认是<code>classpath:/static/error/</code> 路径下定义相关页面：</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314210334.png" alt="" loading="lazy"></figure>
<p>此时，启动项目，如果项目抛出 500 请求错误，就会自动展示 500.html 这个页面，发生 404 就会展示 404.html 页面。如果异常展示页面既存在 5xx.html，也存在 500.html ，此时，发生500异常时，优先展示 500.html 页面。</p>
<p><font color="green" size=5>二、动态异常页面</font></p>
<p>动态的异常页面定义方式和静态的基本 一致，可以采用的页面模板有 jsp、freemarker、thymeleaf。动态异常页面，也支持 404.html 或者 4xx.html ，但是一般来说，由于动态异常页面可以直接展示异常详细信息，所以就没有必要挨个枚举错误了 ，直接定义 4xx.html（这里使用thymeleaf模板）或者 5xx.html 即可。</p>
<p><em><strong>注意，动态页面模板，不需要开发者自己去定义控制器，直接定义异常页面即可 ，Spring Boot 中自带的异常处理器会自动查找到异常页面。</strong></em></p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314210533.png" alt="" loading="lazy"></figure>
<pre><code class="language-html">    &lt;!DOCTYPE html&gt;

    &lt;html lang=&quot;en&quot; xmlns:th=&quot;http://www.thymeleaf.org&quot;&gt;

    &lt;head&gt;

        &lt;meta charset=&quot;UTF-8&quot;&gt;

        &lt;title&gt;Title&lt;/title&gt;

    &lt;/head&gt;

    &lt;body&gt;

    &lt;h1&gt;5xx&lt;/h1&gt;

    &lt;table border=&quot;1&quot;&gt;

        &lt;tr&gt;

            &lt;td&gt;path&lt;/td&gt;

            &lt;td th:text=&quot;${path}&quot;&gt;&lt;/td&gt;

        &lt;/tr&gt;

        &lt;tr&gt;

            &lt;td&gt;error&lt;/td&gt;

            &lt;td th:text=&quot;${error}&quot;&gt;&lt;/td&gt;

        &lt;/tr&gt;

        &lt;tr&gt;

            &lt;td&gt;message&lt;/td&gt;

            &lt;td th:text=&quot;${message}&quot;&gt;&lt;/td&gt;

        &lt;/tr&gt;

        &lt;tr&gt;

            &lt;td&gt;timestamp&lt;/td&gt;

            &lt;td th:text=&quot;${timestamp}&quot;&gt;&lt;/td&gt;

        &lt;/tr&gt;

        &lt;tr&gt;

            &lt;td&gt;status&lt;/td&gt;

            &lt;td th:text=&quot;${status}&quot;&gt;&lt;/td&gt;

        &lt;/tr&gt;

    &lt;/table&gt;

    &lt;/body&gt;

    &lt;/html&gt;
</code></pre>
<p>默认情况下，完整的异常信息就是这5条，展示 效果如下 ：</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314210825.png" alt="" loading="lazy"></figure>
<p>如果动态页面和静态页面同时定义了异常处理页面，例如 <code>classpath:/static/error/404.html</code> 和 <code>classpath:/templates/error/404.html</code> 同时存在时，默认使用动态页面。即完整的错误页面查找方式应该是这样：</p>
<p><em>发生了500错误--&gt;查找动态 500.html 页面--&gt;查找静态 500.html --&gt; 查找动态 5xx.html--&gt;查找静态 5xx.html。</em></p>
<p><font color="green" size=5>三、自定义异常数据</font></p>
<p>默认情况下，在Spring Boot 中，所有的异常数据其实就是上文所展示出来的5条数据，这5条数据定义在 <code>org.springframework.boot.web.reactive.error.DefaultErrorAttributes</code> 类中，具体定义在 <code>getErrorAttributes</code> 方法中 ：</p>
<pre><code class="language-java">    @Override

    public Map&lt;String, Object&gt; getErrorAttributes(ServerRequest request,

                    boolean includeStackTrace) {

            Map&lt;String, Object&gt; errorAttributes = new LinkedHashMap&lt;&gt;();

            errorAttributes.put(&quot;timestamp&quot;, new Date());

            errorAttributes.put(&quot;path&quot;, request.path());

            Throwable error = getError(request);

            HttpStatus errorStatus = determineHttpStatus(error);

            errorAttributes.put(&quot;status&quot;, errorStatus.value());

            errorAttributes.put(&quot;error&quot;, errorStatus.getReasonPhrase());

            errorAttributes.put(&quot;message&quot;, determineMessage(error));

            handleException(errorAttributes, determineException(error), includeStackTrace);

            return errorAttributes;

    }
</code></pre>
<p>DefaultErrorAttributes 类本身则是在org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration 异常自动配置类中定义的，如果开发者没有自己提供一个 ErrorAttributes 的实例的话，那么 Spring Boot 将自动提供一个ErrorAttributes 的实例，也就是 DefaultErrorAttributes 。</p>
<p><strong>基于此 ，开发者自定义 ErrorAttributes 有两种方式 ：</strong></p>
<ol>
<li>直接实现 ErrorAttributes 接口</li>
<li>继承 DefaultErrorAttributes（推荐），因为 DefaultErrorAttributes 中对异常数据的处理已经完成，开发者可以直接使用。</li>
</ol>
<p>具体定义如下：</p>
<pre><code class="language-java">    @Component
    public class MyErrorAttributes  extends DefaultErrorAttributes {
        @Override
        public Map&lt;String, Object&gt; getErrorAttributes(WebRequest webRequest, boolean includeStackTrace) {
            Map&lt;String, Object&gt; map = super.getErrorAttributes(webRequest, includeStackTrace);
            if ((Integer)map.get(&quot;status&quot;) == 500) {
                map.put(&quot;message&quot;, &quot;服务器内部错误!&quot;);
            }
            return map;
        }

    }
</code></pre>
<p>定义好的 ErrorAttributes 一定要注册成一个 Bean ，这样，Spring Boot 就不会使用默认的 DefaultErrorAttributes 了，运行效果如下图：</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314211458.png" alt="" loading="lazy"></figure>
<p><font color="green" size=5>四、自定义异常视图</font></p>
<p>异常视图默认就是前面所说的静态或者动态页面，这个也是可以自定义的，首先 ，默认的异常视图加载逻辑在 org.springframework.boot.autoconfigure.web.servlet.error.BasicErrorController 类的 errorHtml 方法中，这个方法用来返回异常页面+数据，还有另外一个 error 方法，这个方法用来返回异常数据（如果是 ajax 请求，则该方法会被触发）。</p>
<pre><code class="language-java">    @RequestMapping(produces = MediaType.TEXT_HTML_VALUE)

    public ModelAndView errorHtml(HttpServletRequest request,

                    HttpServletResponse response) {

            HttpStatus status = getStatus(request);

            Map&lt;String, Object&gt; model = Collections.unmodifiableMap(getErrorAttributes(

                            request, isIncludeStackTrace(request, MediaType.TEXT_HTML)));

            response.setStatus(status.value());

            ModelAndView modelAndView = resolveErrorView(request, response, status, model);

            return (modelAndView != null) ? modelAndView : new ModelAndView(&quot;error&quot;, model);

    }
</code></pre>
<p>在该方法中 ，首先会通过 getErrorAttributes 方法去获取异常数据（实际上会调用到 ErrorAttributes 的实例 的 getErrorAttributes 方法），然后调用 resolveErrorView 去创建一个 ModelAndView ，如果这里创建失败，那么用户将会看到默认的错误提示页面。正常情况下， resolveErrorView 方法会来到 DefaultErrorViewResolver 类的 resolveErrorView 方法中：</p>
<pre><code class="language-java">@RequestMapping(produces = MediaType.TEXT_HTML_VALUE)

    public ModelAndView errorHtml(HttpServletRequest request,

                    HttpServletResponse response) {

            HttpStatus status = getStatus(request);

            Map&lt;String, Object&gt; model = Collections.unmodifiableMap(getErrorAttributes(

                            request, isIncludeStackTrace(request, MediaType.TEXT_HTML)));

            response.setStatus(status.value());

            ModelAndView modelAndView = resolveErrorView(request, response, status, model);

            return (modelAndView != null) ? modelAndView : new ModelAndView(&quot;error&quot;, model);

    }   
</code></pre>
<p>在这里，首先以异常响应码作为视图名分别去查找动态页面和静态页面，如果没有查找到，则再以 4xx 或者 5xx 作为视图名再去分别查找动态或者静态页面。要自定义异常视图解析，也很容易 ，由于 DefaultErrorViewResolver 是在 ErrorMvcAutoConfiguration 类中提供的实例，即开发者没有提供相关实例时，会使用默认的 DefaultErrorViewResolver ，开发者提供了自己的 ErrorViewResolver 实例后，默认的配置就会失效，因此，自定义异常视图，只需要提供 一个 ErrorViewResolver 的实例即可：</p>
<pre><code class="language-java">@Componentpublic 
class MyErrorViewResolver extendsDefaultErrorViewResolver {    
    public MyErrorViewResolver(ApplicationContext applicationContext, ResourceProperties resourceProperties) {        
        super(applicationContext, resourceProperties);    
    }    
    @Override    
    public ModelAndView resolveErrorView(HttpServletRequest request, HttpStatus status, Map&lt;String, Object&gt; model) 
    {        
        return new ModelAndView(&quot;/aaa/123&quot;, model);    
    }
}
</code></pre>
<p>实际上，开发者也可以在这里定义异常数据（直接在 resolveErrorView 方法重新定义一个 model ，将参数中的model 数据拷贝过去并修改，注意参数中的 model 类型为 UnmodifiableMap，即不可以直接修改），而不需要自定义MyErrorAttributes。定义完成后，提供一个名为123的视图，如下图：</p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/Gentleelephant/picgo/master/img/20200314212941.png" alt="" loading="lazy"></figure>
]]></content>
    </entry>
</feed>